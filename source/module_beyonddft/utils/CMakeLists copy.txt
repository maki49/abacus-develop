
if(ENABLE_LCAO)
  list(APPEND objects
      density/dm_trans_serial.cpp
      density/dm_trans_parallel.cpp
      utils/lr_util_algorithm.cpp
      utils/lr_util_physics.cpp
  )
  add_library(
    beyonddft
    OBJECT
    ${objects}
  )
if(BUILD_TESTING)
  add_subdirectory(test)
endif()
  if(ENABLE_COVERAGE)
    add_coverage(beyonddft)
  endif()
endif()





